CREATE TABLE PERSONS
(
    PERSON_ID  NUMBER GENERATED BY DEFAULT AS IDENTITY (START WITH 1 INCREMENT BY 1) NOT NULL PRIMARY KEY,
    FIRST_NAME NUMBER                                                                NOT NULL,
    LAST_NAME  VARCHAR2(100)                                                         NOT NULL,
    CONSTRAINT PERSON_ID_UINT CHECK ( PERSON_ID BETWEEN 0 AND 4294967295)
);

CREATE INDEX INDEX_ON_TABLE_PERSONS ON PERSONS (PERSON_ID, FIRST_NAME);

DROP INDEX INDEX_ON_TABLE_PERSONS;

CREATE TABLE MEGATABLE
(
    NIGGER_ID     NUMBER GENERATED BY DEFAULT AS IDENTITY (START WITH 1 INCREMENT BY 1) NOT NULL PRIMARY KEY,
    SLAVE_NAME    VARCHAR2(100)                                                         NOT NULL,
    SLAVE_SURNAME VARCHAR2(100)                                                         NOT NULL,
    CONSTRAINT NIGGER_ID_UINT CHECK ( NIGGER_ID BETWEEN 0 AND 4294967295),
    TMP NUMBER NOT NULL,
    CONSTRAINT FK_TEST_ME FOREIGN KEY (TMP) REFERENCES PERSONS(PERSON_ID)

);

DROP TABLE MEGATABLE;

CREATE OR REPLACE PROCEDURE TEST_PROCEDURE(TMP1 NUMBER) IS
BEGIN
    NULL;
END TEST_PROCEDURE;

CREATE OR REPLACE FUNCTION TEST_FUNCTION(TMP1 VARCHAR2) RETURN NUMBER IS
BEGIN
    return 10;
END TEST_FUNCTION;

CREATE PACKAGE PAKETIK AS
    FUNCTION p_strng RETURN VARCHAR2;
END PAKETIK;
